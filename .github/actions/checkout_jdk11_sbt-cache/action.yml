name: 'setup jdk11 and sbt cache'
description: 'Checkout Repo / Setup JDK 11 / sbt-cache by SO'

inputs:
  token:
    required: true
    description: 'token to checkout repo'
  fetch-depth:
    required: false
    description: 'fetch-depth to checkout repo'
    default: '1'

runs:
  using: "composite"
  steps:
    - name: Clone Repository
      uses: actions/checkout@v3
      with:
        fetch-depth: ${{ inputs.fetch-depth }}
        token: ${{ inputs.token }}

    - name: Set up JDK 11
      uses: actions/setup-java@v1
      with:
        distribution: 'adopt'
        java-version: '11'

    - name: SBT Cache
      uses: actions/cache@v3
      with:
        path: |
          ~/.sbt
          ~/.ivy2/cache
          ~/.coursier
          ~/.cache/coursier
        key: sbt-cache-${{ runner.os }}-${{ matrix.target-platform }}-${{ hashFiles('project/build.properties') }}
